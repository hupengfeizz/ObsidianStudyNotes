> ✅ 已由 Code 窗口处理归档（2026-08-28）：指令1/3 已执行，指令2 答"重训尚未启动"；审计结果见当轮回复与实验记录[11]补记。

# 交接纪要 2026-08-28 T1-A 重训逻辑讨论

## 一、确认打通的理解（≤5条）

1. 用户已理解:T1-A 重训与形状统计脚本**没有因果关系**,是两条独立的线(重训 = 学习率决策链;统计脚本 = 第 1 站解剖实验)。
2. 用户已理解:崩溃后不立刻重训的原因——崩溃只说明 3e-5 太大,不说明该换成多少;先用 2 轮短跑重扫 {1e-5, 1.5e-5, 2e-5} 是为了便宜地定数,再押 20 轮的长训。
3. 用户已理解:学习率重扫没有专用脚本,就是**同一个训练脚本跑三次短的**(轮数 20→2,lr 依次改),与正式启动命令是同一套参数底稿。
4. 用户已确认:手上的正式启动命令里 lr = 1e-5,与重扫结论一致。

## 二、暴露的薄弱点 / 新疑问（Code 窗口需要针对性补的）

1. **记忆冲突待核验(本纪要核心)**:用户记得"重扫选定 1e-5 之后,跑 1e-5 又崩掉了一次"。但背景包记录是:1e-5 重扫 2 轮健康(iou 0.223),真正 NaN 崩溃的只有首训 3e-5 那次(第 5 轮峰值 2e-4)。**请 Code 窗口对照真实训练日志核对:1e-5 是否发生过任何一次崩溃/NaN/中断**。两个候选解释:
   - (a) 用户把 1.5e-5 那次 iou 0.016 的"反常烂"记成了崩溃(数字上确实像崩,但性质是坏初始化,不是发散);
   - (b) 背景包同步之后又发生了新事件,尚未蒸馏进来。
2. 用户对统计脚本(tools 下 anatomy/形状打印类脚本)的输出各列含义仍不熟悉:points (N,5)、voxels (M,20,5)、num_pts 统计、非空格子数、每柱均点、满载率、丢点率——知道有这些列,但每列"为什么要看它"还说不完整。建议下次讨论或 Code 端补一次逐列走读。

## 三、给 Code 窗口的指令与计划

1. 核对 T1-A 全部训练记录(含重扫的三次 2 轮短跑 + 正式重训),回答:**lr 1e-5 下是否出现过崩溃/NaN**?若有,把时间、轮次、现象写进台账并同步进下一版背景包;若无,在台账里明确写一句"1e-5 从未崩溃",帮用户钉死这条记忆。
2. 若正式重训(lr 1e-5, 20 轮)已在进行:确认当前进度与 iou 曲线是否健康,写进下一版背景包。
3. 背景包下一版建议补一节"统计脚本输出逐列说明"(每列名称 → 含义 → 为什么看它),对应薄弱点 2。

## 四、用户点名要记的原话

(本次无"这条记下来"的点名内容)

## 五、本次讨论的关键论断清单（供 Code 窗口审计）

1. T1-A 首训崩溃原因 = 沿用 lr 3e-5,cyclic 峰值爬到 2e-4 越过单帧发散阈值,第 5 轮 NaN;前 4 轮 iou 仅 0.016。[背景包]
2. 重训与首训唯一区别 = 学习率 3e-5 → 1e-5,模型/数据/单帧输入全部不变。[背景包]
3. 重扫结果:1e-5 健康(0.223)、2e-5 略高(0.236)但峰值贴发散线、1.5e-5 反常(0.016)且已验尸确认为坏初始化而非 lr 问题。[背景包]
4. 学习率重扫的实现方式 = 同一训练脚本改 lr 与轮数跑三次,无专用重扫脚本。[推测]——与背景包"重扫 {1e-5,1.5e-5,2e-5} 各 2 轮"的描述一致,但具体命令形式请 Code 窗口确认。
5. 用户运行的 tools 下统计/anatomy 脚本 = 第 1 站体素化的形状打印实验,产出 N/M/均点/满载率/丢点率数据,不训练、不更新权重,与 T1 训练决策链无关。[背景包]
6. "1e-5 又崩了一次"这条用户记忆,背景包无对应记录,真伪待 Code 窗口核验。[推测]
